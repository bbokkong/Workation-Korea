# Workation Korea 업데이트 v2

이 압축파일은 기존 GitHub 저장소에 **덮어쓰기용**입니다. `site/data/programs.json`은 포함하지 않아 현재 수집된 93개 프로그램 데이터를 지우지 않습니다.

## 이번 업데이트

1. **모집상태 정확도 개선**
   - 페이지 전체의 `신청하기` 문구만 보고 모집중으로 판정하지 않습니다.
   - 프로그램명 주변 문맥과 신청/마감 문구를 함께 확인합니다.
   - 과거 마감일이면 모집중으로 표시하지 않습니다.
   - `status_verified`와 신뢰도를 저장합니다.
   - 공식적으로 확인된 모집중만 `공식확인` 배지가 표시됩니다.

2. **대한민국 행정구역 클릭 지도 개선**
   - 지도 이동 범위를 남한 중심으로 제한했습니다.
   - 시·도 클릭 → 시·군·구 클릭의 2단계 필터가 작동합니다.
   - 선택한 행정구역을 강조하고 지역별 프로그램 수를 툴팁에 표시합니다.

3. **모바일 최적화**
   - 검색/필터는 가로 스크롤형으로 변경됩니다.
   - 지도 높이, 카드 크기, 버튼 배치가 휴대폰에 맞게 자동 조정됩니다.
   - 프로그램 카드가 작은 화면에서 한 열로 표시됩니다.

## 적용 방법

1. 이 ZIP을 컴퓨터에서 압축 해제합니다.
2. GitHub의 `Workation-Korea` 저장소 → **Code** → **Add file** → **Upload files**로 이동합니다.
3. 압축을 푼 폴더 안의 `scripts`, `site`, `index.html`을 한꺼번에 드래그합니다.
4. 같은 이름의 파일을 교체하도록 업로드한 뒤 **Commit changes**를 누릅니다.
5. **Actions → Update workation data and deploy Pages → Run workflow**에서 `0`을 입력해 전체 93개를 한 번 다시 확인합니다.
6. 완료 후 공개 사이트를 `Ctrl+F5`로 새로고침합니다.

> 중요: `site/data/programs.json`과 `site/data/sync_status.json`은 현재 운영 데이터를 보존하기 위해 이 업데이트 ZIP에 들어 있지 않습니다.
